import { CustomFileSizePipePipe } from './custom-file-size-pipe.pipe';

describe('CustomFileSizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFileSizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
